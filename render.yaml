# Render.com 배포 설정 (Blueprint)
# https://render.com → New → Blueprint → 이 저장소 선택
services:
  - type: web
    name: projectory-event-demo
    runtime: node
    plan: free
    branch: claude/sleepy-goodall-PwIEK
    buildCommand: ""        # 외부 의존성 없음
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: "20"
